{% extends 'base.html.twig' %}

{% block body %}
    <form method="POST" action="/test/result">
        {% for key, question in test %}
            <div>
                {{ key + 1 }}) <label for="answer">{{ question.title }}</label>
                {% for answer in question.answers %}
                    <div>
                        <input id="answer" name="{{ answer.title }}" type="checkbox"> {{ answer.title }}
                    </div>
                {% endfor %}
            </div>
        {% else %}
        <div>
            Не обнаружено вопросов.
        </div>
        {% endfor %}

        <button type="submit">Отправить</button>
    </form>
{% endblock %}